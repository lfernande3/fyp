# Minimal dependencies for simulator, tests, and O5 analysis (no Jupyter).
# Use when `pip install -r requirements.txt` fails on Windows (long path limit
# on notebook labextensions). Enable long paths or install notebook separately.
#
# Install: python -m venv .venv && .venv\Scripts\pip install -r requirements-core.txt

simpy>=4.0.1
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0
matplotlib>=3.4.0
pytest>=7.0.0
seaborn>=0.11.0
ipywidgets>=7.6.0
ipython>=7.25.0

# Jupyter UI (often fails on Windows without long paths enabled):
#   notebook>=6.4.0
